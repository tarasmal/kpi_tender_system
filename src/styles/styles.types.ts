import {
    BorderProps,
    ColorProps,
    LayoutProps,
    PositionProps,
    SizeProps,
    SpaceProps,
    TypographyProps
} from "styled-system";

export type CssProps =
    & BorderProps
    & PositionProps
    & TypographyProps
    & SizeProps
    & SpaceProps
    & LayoutProps
    & ColorProps
